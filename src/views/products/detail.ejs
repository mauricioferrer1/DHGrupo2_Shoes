<!DOCTYPE html>
<html lang="es">
<head>
    
    <%- include('../partials/head') %>

    <link rel="stylesheet" href="/css/detalleproducto.css">
    <title>The Shoes Company</title>

</head>
<body>

    <%- include('../partials/header') %>

<main>
   
    <div class = "fotos360" >
        <img class ="img360" src="/images/products/<%=product.image1%>" >
        <img class ="img360" src="/images/products/<%=product.image2%>" >            
    </div>
    <div class ="fotodetalle">
        <img class ="mainphoto" src="/images/products/<%=product.image%>" >
            <p><%=product.description%></p><br>
    </div>
    <div class = "NombrePagoColorTalla" >
        <p class="nombre-producto"><%=product.name%></p><br>
        <p class="precio-producto">$<%=product.price%></p>
        <br><br>
        <p>Color:</p>
        <section class="colores">
            <% for (let i=0; i< product.colors.length; i++) {%>
                <label>
                    <input type="radio" name="colores" value="<%= product.colors[i].color %>" checked>
                        <img src="/images/products/suede <%= product.colors[i].color%>.jpg">
                </label>
            <% }%>                
        </section
        <p>Talla :</p>
        <div class="talla" style="width:200px;">                
            <select id ="talla">
                <% for (let i=0; i< product.sizes.length; i++) {%>
                    <option value='<%= product.sizes[i].size %>'><%= product.sizes[i].size %></option>
                <% }%>
            </select>
        </div>
        <br>
        <button type="submit"  class="add-cart" onclick="alert('Hemos añadido tu producto al carrito ')"><em>Agregar al carrito </em></button>
    </div>
   
<!--<% //if (locals.islogged) { %> -->
        <a href="/product/<%= product.id %>/editproduct"><button class="Editar"><em>Edit Product</em></button> </a>
        <form action="/eliminar/<%= product.id %>/process?_method=DELETE" method="POST">
            <button type="submit" class="delete"> <em>Borrar producto</em></button>
        </form>
<!--<%// } %> -->
</main>

<%- include('../partials/footer') %>
<script src="/js/agregarAlCarrito.js"></script>
</body>
</html>